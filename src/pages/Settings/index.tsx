import styles from './index.module.css';

// 设置页面
const Settings = () => {
  return (
    <div className={styles['settings-container']}>
      <h1>Settings</h1>
    </div>
  );
};

export default Settings;
